apiVersion: v1
kind: Service
metadata:
  name: fillit-service
spec:
  type: NodePort
  selector:
    app: fillit
  ports:
    - protocol: TCP
      port: 8080         # 클러스터 내 서비스 포트
      targetPort: 8080   # 컨테이너 포트
      nodePort: 8090     # 외부에서 접근할 포트 (노드 포트)