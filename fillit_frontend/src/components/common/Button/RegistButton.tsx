import { RegistIcon } from '@/assets/assets';

const RegistButton = () => {
  return (
    <button>
      <img src={RegistIcon} alt="regist icon" />
    </button>
  );
};

export default RegistButton;
